import { useRequests, useAuth } from "../../contexts";

function FriendRequests({accept, decline}) {
    return (
        <div className="friend-requests-list">
            
        </div>
    )
}

export default FriendRequests
